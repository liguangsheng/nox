[package]
name = "nox"
edition.workspace = true
license.workspace = true
version.workspace = true

[dependencies]
nox_core = { path = "../nox_core" }

[dev-dependencies]
criterion = "0.5"

[[bench]]
name = "runtime_capabilities"
harness = false
