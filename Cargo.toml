[workspace]
members = [
    "crates/nox",
    "crates/nox_core",
]
exclude = ["fuzz"]
resolver = "2"

[workspace.package]
edition = "2021"
license = "MIT"
version = "0.0.6"

[profile.release]
codegen-units = 1
lto = true
strip = "symbols"
