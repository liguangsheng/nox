record User {
    name: str,
    score: int,
}

fn score_with_bonus(user: User, bonus: int) -> int {
    return user.score + bonus;
}

let user: User = User { name: "nox", score: 40 };
user.score_with_bonus(2);
